# AWS Deployment Environment Variables
# Copy this to .env and fill in your values

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=tweet_sentiment_db

# API Keys
X_API_BEARER_TOKEN=your_x_api_bearer_token_here
GROK_API_KEY=your_grok_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Application Configuration
NODE_ENV=production
PORT=9000

# Enable scraper in AWS (since we have browser support)
USE_X_API=true
ENABLE_SCRAPER_FALLBACK=true

# Process tweets automatically
PROCESS_TWEETS=true

# Optional: Redis for caching (if you want to add Redis)
# REDIS_HOST=localhost
# REDIS_PORT=6379
